# data structure like array, but cannot contain duplicates, is unordered
# uses hashing to do lookups in constant time
# like dictionary, but without key-value pair

import timeit

print set("abcdefgefg")